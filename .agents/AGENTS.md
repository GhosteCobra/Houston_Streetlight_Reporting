# Source bundle handling

Read the root [AGENTS.md](../AGENTS.md) first. This directory contains the user's reference skill collection and its provenance. Preserve imported files byte-for-byte unless the task explicitly updates the bundle. The original skills/AGENTS.md describes that source collection; it does not override the project's develop workflow, release authorization, or tool/evidence boundaries. Apply only the relevant skills under the root project overrides. Do not execute or install bundled helpers as an import step.
