# Camera capture and preview

**Owner:** Person 5. **Status:** documentation placeholder; implementation pending.

## Purpose

Phone capture, upload fallback, preview, retake, remove, and no-photo recovery.

## Planned contents

Future CameraCapture and PhotoPreview components.

## Working rules

Emit a File or null to the draft owner. Validate picker inputs, clean object URLs/tracks, and rely on server validation before persistence.

Use the [team guides](../../../docs/README.md) for dependencies, acceptance criteria, and workflow, and the [shared contract](../../../docs/data-contract.md) for field definitions. Update this folder guide with actual entry points and verified commands as code is added.
