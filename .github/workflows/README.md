# CI workflow plan

**Owner:** Person 5. **Status:** documentation placeholder; implementation pending.

## Purpose

Automate checks on implementation PRs and releases once the app exists.

## Planned contents

Future CI YAML: clean npm install, formatting, lint, types, tests and production build.

## Working rules

This Markdown file executes nothing. Pin compatible runtime/dependencies; avoid giving untrusted PRs secrets. Record real check names before making them required.

Use the [team guides](../../docs/README.md) for dependencies, acceptance criteria, and workflow, and the [shared contract](../../docs/data-contract.md) for field definitions. Update this folder guide with actual entry points and verified commands as code is added.
